import React from "react";
import CameraTemplate from "../../templates/CameraTemplate";

const CameraPage: React.FC = (): React.ReactElement => {
  return <CameraTemplate />;
};

export default CameraPage;
